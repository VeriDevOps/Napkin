/*
import Vue from 'vue'
import app from '../js/App.vue'

describe('App', function() {

	beforeEach(function() {

	});

	it('The welcome message should be defined', function() {
		var msg = app.data().msgWelcome;
		expect(msg).toBeDefined();
		expect(msg).toEqual('Welcome to the SAGA Toolbox!')
	});

	it('The signal selection message should be defined', function() {
		var msg = app.data().msgSelectSignals;
		expect(msg).toBeDefined();
		expect(msg).toEqual('Select signals in the list to plot.')
	});

});
*/
